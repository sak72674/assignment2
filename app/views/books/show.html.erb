<% if flash[:notice] %>
    <div class="flash">
       <%= flash[:notice] %>
    </div>
<% end %>

<p class="show-item">Title: <%= @book.title %></p>
<p class="show-item">Body: <%= @book.body %></p>
<%= link_to 'Edit', edit_book_path(@book.id) %>
<%= link_to 'Back', books_path %>